{ct_opts, [{ct_hooks, [cth_readable_shell]}]}.

{erl_opts, [debug_info, warnings_as_errors]}.

{plugins, [rebar3_proper]}.

{profiles, [
  {test, [
    {deps, [
      cth_readable,
      proper,
      recon
    ]},

    {cover_enabled, true}
  ]}
]}.
